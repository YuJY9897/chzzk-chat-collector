# Changelog

이 프로젝트의 주요 변경사항을 기록합니다.

## 2026-09-04 (21차) — 수집 시뮬레이터

- `tools/simulate-collection.mjs` 추가. 방송을 켜지 않고 수집 한 판을 통째로 검증한다
- 치지직 소켓만 가짜로 두고 파일 생성 → 채팅 기록 → 일시정지/재개 마커 → 종료 → 하이라이트 분석까지 실제 코드로 실행
- 함께 검사하는 것: CSV 헤더 최신 여부, 줄 수와 카운터 일치, `elapsed_seconds`/`user_role` 채워짐, JSONL 마커, 발화자 ID 해시, 채팅 0줄일 때 빈 파일 삭제, 시작 실패 시 뒷정리
- 13개 항목 전부 통과 확인

## 2026-09-04 (20차) — 대표 반응 묶기, elapsed_seconds 기본값

- 하이라이트 대표 반응이 `ㅋㅋㅋ`와 `ㅋㅋㅋㅋㅋㅋ`를 다른 문자열로 세던 문제 수정. 3회 이상 반복 문자를 3개로 줄여 묶고, 가장 짧은 표기를 대표로 보여준다
- 실제 방송에서는 똑같은 문장이 반복되는 경우가 드물어, 횟수가 같으면 긴 문장을 우선하도록 변경 (짧은 무의미 문자열 대신 실제 대화가 보인다)
- `elapsed_seconds`가 계속 비어 있던 문제: 시작 시각을 입력하지 않으면 **수집을 켠 시각** 기준으로 계산한다. 다시보기 타임라인에 정확히 맞추려면 여전히 직접 입력이 필요

## 2026-09-04 (19차) — v0.3.0 exe 재빌드

- 버전 0.2.0 → 0.3.0
- `dist/CHZZK-Clip-Scout.exe` 재빌드 (기존 exe는 7월 16일 빌드라 오늘 변경분이 하나도 없었음)
- 패키지 안의 코드가 소스와 일치하는지 파일 단위로 대조 확인, 빌드된 exe를 실제로 실행해 동작 확인
- 포터블 exe는 `.env`(와 `tokens.json`)를 exe와 같은 폴더에 두어야 계정 연결이 유지됨

## 2026-09-04 (18차) — Electron 실행 검증, 문서 갱신

- Electron 앱을 실제로 실행해 개편된 화면이 정상 동작하는 것을 확인 (렌더러 오류 없음, 창 캡처로 육안 확인)
- 스모크 테스트(`SMOKE_TEST=1 npx electron .`)가 개편 전 요소(`status-text`, `connect-area`)를 참조해 실패하던 것 수정 → `hero-title`, `hero-sub`, 접이식 섹션 수를 확인하도록 변경
- README를 실제 동작에 맞게 갱신: 방송 시작/종료 자동 감지 서술 삭제, 그 근거가 된 실측 결과를 "중요한 제한"에 기록, CSV 컬럼 최신화, 하이라이트 감지·더미 생성기 사용법 추가

## 2026-09-04 (17차) — 메인 화면 단순화

- 메인에는 현재 상태 한 줄과 수집 ON/일시정지/종료 버튼만 남김
- 치지직 연결 / 저장 설정 / 마지막 수집 결과 / 실시간 채팅은 접이식으로 내려 필요할 때만 펼치도록 변경
- 접힌 헤더에 요약 표시 (연결됨, 225줄, 하이라이트 2개 등)
- 상태 문구를 실제로 알 수 있는 것만 쓰도록 정리
  - `방송 채팅 수신 중` — 채팅이 들어오는 중이면 방송 중인 것이 확실
  - `수집 중 — 채팅 없음` — 2분 이상 조용함. 방송 종료인지 조용한 방송인지는 알 수 없음
  - `수집 중 — 채팅 대기`, `대기 중`, `일시정지`, `치지직 계정 연결이 필요합니다`
- 미연결 상태면 "치지직 연결" 섹션이 자동으로 펼쳐진다
- 웹/Electron 양쪽 동일하게 적용

## 2026-09-04 (16차) — 방송 자동 감지 철회 (동작하지 않음)

- **15차에서 "성공"으로 보고한 방송 종료 자동 감지는 오탐이었다.** 실제로는 방송 중에도 1분 뒤 멋대로 종료됨
- 원인: 소켓에 접속하지 않은 임시 세션은 서버에 등록되지 않아 구독이 항상 404. 방송 여부와 무관했음
- 추가 확인: 소켓에 접속한 세션이면 **방송이 꺼져 있어도 구독이 200으로 성공**한다. 즉 채팅 구독은 방송 상태와 아무 관련이 없다
  - 소켓 미접속 세션 → 404 NOT_FOUND (세션 없음)
  - 소켓 접속 세션 → 200 SUCCESS (방송 on/off 무관), 중복 구독 시 409
- 이에 따라 기존 "구독 실패 = 방송 전" 가정도 틀렸음. 방송 시작 감지와 시작 시각 자동 기록도 실제로는 동작한 적이 없다
- 공식 API에 내 채널의 방송 상태를 알려주는 엔드포인트가 없음 (Channel/lives/setting 모두 확인)
- 조치: 잘못된 자동 종료 로직 제거, 거짓 상태 문구("이미 방송 중이라...", "방송 시작을 감지했습니다") 삭제. 종료는 사용자가 직접 해야 한다
- `revoked` 이벤트 처리는 유지 (오면 즉시 반응, 실제로는 오지 않음)
- 연결 표시가 "마지막 연결 2026. 9. 4. (0일 전). 오래되면 만료되어 재연결이 필요합니다"처럼 당장 재연결이 필요한 것처럼 보이던 문구 수정 → "마지막 연결 오늘", 14일 이상일 때만 안내 문구 추가

## 2026-09-04 (15차) — 실측 검증, 시작 실패 시 빈 파일 정리

- 실제 방송으로 **방송 종료 자동 감지 첫 성공**: 방송을 끄고 약 1분 뒤 `broadcast_end`로 자동 마감 확인
- 실제 시청자 채팅을 처음 수집: `userRoleCode`가 `common_user`로 들어오고 `profile` 안에 있는 것, 발화자별 해시가 분리되는 것 확인
- 수집 시작에 실패하면(토큰 만료 등) 헤더만 있는 빈 CSV/JSONL이 `data/`에 남던 문제 수정
  - 파일을 만든 뒤 소켓에 연결하는 구조라, 연결 단계에서 실패하면 아무도 그 파일을 지우지 않았음
  - `start()`가 실패하면 빈 파일을 정리하고 에러를 그대로 올린다 (이때는 상태 문구를 띄우지 않음)

## 2026-08-18 (14차) — 수집 종료 시 하이라이트 자동 분석

- 수집이 끝나면 저장된 CSV를 자동 분석해 하이라이트 상위 5개를 결과 카드에 표시 (웹/Electron 양쪽)
- 구간별로 시각, 길이, 분당 채팅 변화(평상시→최고), 대표 반응 3개를 보여준다
- CSV 로더를 `tools/`에서 `src/highlight.js`로 옮겨 앱과 CLI가 같은 코드를 쓰도록 정리
- `analyzeFile()`은 어떤 경우에도 예외를 던지지 않는다 (파일 없음/빈 로그면 빈 배열)
- 시간 표기가 3시간 방송에서 `137:40`처럼 깨지던 것 수정 → `2:17:40`

## 2026-08-18 (13차) — 방송 종료 자동 감지 (실측 기반)

- 실측 결과: 방송을 꺼도 `revoked` 이벤트가 오지 않고 소켓도 끊기지 않아, 종료 버튼을 누를 때까지 계속 "수집 중"으로 남아 있었음
- 세션 목록(`GET /open/v1/sessions`)의 `subscribedEvents`도 종료 후 그대로 남아 판별에 쓸 수 없음을 확인
- 해결: 1분마다 임시 세션으로 채팅 구독을 시도해 방송 여부를 판별 (방송 중이 아니면 404). 임시 세션은 소켓 접속을 하지 않아 세션 목록에 쌓이지 않고, 진행 중인 구독도 건드리지 않음
- 최근 30초 안에 채팅이 들어왔으면 확인을 건너뛴다 (불필요한 호출 방지)
- 일시적 오류(500/네트워크)는 종료로 보지 않고 다음 확인으로 넘긴다. 401이면 `auth_expired`로 종료
- `revoked` 처리도 그대로 유지 (혹시 오는 경우 즉시 반응)
- `src/http.js`의 `isAuthError` 정규식에 제어문자가 섞여 401 판별이 사실상 동작하지 않던 문제 수정

## 2026-08-18 (12차) — 방송 시작/종료 감지 개선

- 방송 시작 확인 주기 30초 고정 → 2분간 5초, 10분까지 15초, 이후 30초 (방송 직전일수록 촘촘히)
- 구독 실패 원인을 구분: 토큰 만료(401)면 기다려도 소용없으므로 즉시 `auth_expired`로 종료하고 계정 연결 해제
- 상태 문구를 사용자 언어로 교체: "채팅 구독 완료" → "방송 시작을 감지했습니다", "이미 방송 중이라 바로 수집을 시작합니다"
- 방송 시작 시각 자동 기록: 구독 실패→성공으로 바뀐 순간을 방송 시작으로 보고 `elapsed_seconds`를 자동 계산 (직접 입력한 값이 있으면 그것을 우선)
- 방송 종료는 기존대로 세션 `revoked` 이벤트로 실시간 감지 (치지직 Open API에는 방송 시작 이벤트가 없어 시작만 폴링)

## 2026-08-18 (11차) — 하이라이트 감지

- `src/highlight.js` 추가. 채팅 로그에서 클립각 구간을 찾는다
- 채팅량만 보면 인사 타임에도 걸리므로 3가지를 함께 본다: 평상시 대비 채팅 폭증, 반응성 표현 비율(ㅋㅋㅋ/클립/헐/레전드 등), 참여자 수 급증
- 기준선은 앞뒤 10분 중앙값 — 방송 중 시청자가 늘고 줄어도 따라간다
- 채팅이 적은 방송에서 몇 줄만 몰려도 배수가 튀는 문제 → 절대량 하한(`minChats`) 추가
- 탐지 구간은 앞 20초를 덧붙여 클립 맥락을 담고, 구간별 대표 반응 3개를 함께 출력
- `tools/detect-highlights.mjs`로 실행, 더미 정답지가 있으면 재현율/정밀도 자동 채점

## 2026-08-18 (10차) — 더미 로그 생성기

- `tools/make-dummy-log.mjs` 추가. 분석/UI 개발용 가짜 채팅 로그를 실제와 같은 포맷으로 생성
- 포맷 어긋남을 막으려고 CSV/JSONL 쓰기는 `ChatCollector.writeChat`을 그대로 재사용
- 하이라이트 구간 5개를 의도적으로 심고 `*.highlights.json`에 정답지를 함께 저장 (감지 알고리즘 채점용)
- 채팅량을 동시시청자 수에서 역산(분당 채팅 ≈ 시청자의 20%), 발화 빈도는 Zipf-Mandelbrot 분포로 헤비 채터/눈팅족 재현
- 사용법: `node tools/make-dummy-log.mjs [출력폴더] [동시시청자] [분]` (기본 300명 / 180분)
- 출력 폴더 `test-data/`는 .gitignore 처리

## 2026-08-18 (9차) — 첫 실측 반영, CSV 스키마 정리

- 실제 방송 수집 성공 후 응답 구조 확정: `userRoleCode`는 `profile` 안에 있음(공식 문서 표기와 다름), 값은 소문자(`streamer`)
- `chat_channel_id` 컬럼 추가 (CHAT 이벤트의 `chatChannelId`, 다시보기 연결용)
- `badge_count`(개수) → `badges`(종류) 로 변경. `imageUrl`의 파일명을 뽑아 `streamer` 형태로 저장
- 수집된 채팅이 0줄이면 헤더만 남은 빈 CSV/JSONL을 저장하지 않고 삭제
- 문서에 없는 `eventSentAt` 필드가 실제로 내려옴(KST, 타임존 표기 없음) — JSONL 원본에는 그대로 보존

## 2026-08-18 (8차) — 토큰 만료 안내

- 토큰이 만료된 상태로 수집 ON을 누르면 `401 INVALID_TOKEN` 원문이 그대로 노출되던 문제 수정
- 401/INVALID_TOKEN 감지 시 "치지직 연결이 만료되었습니다. 계정을 다시 연결해 주세요" 안내 + 만료 토큰 자동 정리(UI가 즉시 미연결 상태로 전환)
- 토큰 저장 시 `savedAt` 기록, 연결 영역에 "마지막 연결 YYYY-MM-DD (N일 전)" 표시
- 웹 버전(server.js)과 Electron 앱 양쪽 모두 적용

## 2026-08-18 (7차) — 채팅 작성자 등급 파싱 수정

- `user_role` 컬럼이 항상 비어 나오던 문제 수정: 치지직 공식 문서상 `userRoleCode`는 CHAT 이벤트 최상위 필드인데 `profile` 안에서만 찾고 있었음
- 최상위 → `profile` 순으로 조회하도록 변경 (실제 응답이 어느 쪽이든 값을 놓치지 않음)
- 실제 방송 수집 실측 전이라 문서 기준 대응. 실측 후 재확인 필요

## 2026-07-12 (6차) — Electron 데스크톱 앱 (v0.2.0)

- 브라우저 UI를 대체하는 네이티브 데스크톱 앱 추가 (`npm run app`, exe 빌드는 `npm run dist`)
- 기존 기능 전부 이식: 계정 연결, 로그 수집 ON/일시정지/재개/종료, 방송 종료 자동 감지, 저장 경로 선택(네이티브 폴더 창)
- 창 닫기 시 "백그라운드로 유지 / 완전히 종료 / 취소" 선택 대화상자 + "이 선택 기억하기" (트레이 메뉴에서 초기화 가능)
- 트레이 아이콘 내장 (상태 표시, 더블클릭 열기, 앱 종료), 수집 종료 시 Windows 알림
- 실시간 채팅 목록 (IPC 푸시 방식이라 새로고침 불필요), 마지막 수신 상대 시각 표시
- 포터블 exe 패키징 (dist/CHZZK-Clip-Scout.exe, .env를 exe 옆에 두고 실행)
- 웹 버전(npm run server)은 레거시로 유지

## 2026-07-12 (5차) — 저장 위치 폴더 선택

- 저장 위치를 직접 타이핑하는 대신 Windows 기본 폴더 선택 창으로 고르도록 변경 (경로 입력칸은 읽기 전용)
- 지정한 저장 위치를 탐색기로 바로 여는 "열기" 버튼 추가
- 한글 경로 인코딩 무손실 확인, 선택 취소 시 기존 경로 유지

## 2026-07-12 (4차) — 트레이 아이콘

- 백그라운드 실행 중임을 보여주는 트레이 아이콘 추가 (초록 점, 작업표시줄 오른쪽 아래)
- 더블클릭 → 화면 열기, 우클릭 메뉴 → 열기 / 앱 종료
- 서버가 종료되면 트레이 아이콘도 자동으로 사라짐, 중복 실행 방지
- 추가 설치 불필요 (Windows 내장 PowerShell + .NET Forms 사용)

## 2026-07-12 (3차) — 사용자 관점 다듬기

- UI 전면 리디자인: 카드형 레이아웃, 상태 배지(수집 중 펄스 표시), 다듬어진 버튼/입력 스타일
- 수집 종료 알림을 브라우저 기본 alert 대신 앱 내 모달로 교체 (저장 위치 표시 + 저장 폴더 열기 버튼)
- "저장 폴더 열기" 기능 추가 — 수집 결과 파일을 탐색기에서 바로 확인
- 저장 위치 기본값을 절대경로로 표시해 어디에 저장되는지 명확히
- 최근 채팅/수집 시각을 한국 시간(HH:MM:SS)으로 표시, 마지막 수신은 "N초 전" 상대 시간
- "다시보기 기준 시작 시간" 입력을 고급 설정으로 접어 초심자 혼란 감소
- 계정 미연결 시 로그 수집 ON 버튼 비활성화
- 중복 실행 파일(숨김 실행.vbs) 제거 — cmd 하나로 통일 (자체적으로 백그라운드 실행)
- 전체 플로우 실계정 테스트: 연결 → ON(구독 성공) → 일시정지 → 재개 → 종료 → 폴더 열기 → 앱 종료

## 2026-07-12 (2차) — UI 개편 + 치명적 버그 수정

### 버그 수정 (치명적)
- **치지직 세션 이벤트가 JSON 문자열로 도착하는데 객체로 가정하고 있던 버그 수정.** 이 버그 때문에 기존 버전은 sessionKey를 읽지 못해 실제 환경에서 채팅 구독이 아예 되지 않았음 (6월 테스트 CSV가 비어 있던 원인). 실제 치지직 세션으로 구독 성공 확인 완료.

### UI/UX 개편
- 실행 시 CMD 창 없이 백그라운드로 동작, 웹 화면의 "앱 종료" 버튼으로 종료
- 계정이 연결돼 있으면 연결 버튼 비활성화 + "이미 연결되어 있습니다" 안내
- "자동 저장 예약" → "로그 수집 ON/OFF"로 개념 단순화. 방송 전에 ON 하면 방송 시작 시 자동 수집
- 저장 경로 지정 기능 추가 (수집 중에는 변경 불가)
- "채팅 저장 멈추기" → "일시정지/재개" + "종료" 버튼으로 분리. 일시정지 후 재개하면 같은 파일에 이어서 저장 (JSONL에 pause/resume 마커 기록)
- 방송 종료(구독 해제) 자동 감지 → 수집 자동 종료 + 저장 위치를 알림 창으로 표시
- "최근 채팅" 새로고침 버튼 추가 — 원할 때만 수집 상태/최근 채팅 확인

## 2026-07-12 (1차)

- GitHub 저장소 생성 및 초기 버전 공개
- `.env.example` 추가 (README가 참조하던 누락 파일)
- README에 안정성/개인정보 처리 내용 문서화

## 2026-07-11

### 안정성
- 방송 중 네트워크 순단 시 자동 재연결 추가: 5초 간격, 최대 5분, 같은 파일에 이어쓰기
- 연결 시도 실패(`connect_error`/`connect_timeout`)도 재시도 루프에 포함 — 재연결이 조용히 멈추는 버그 수정
- 재연결 구간을 JSONL에 `reconnect_start`/`reconnect_end` 마커로 기록해 수집 공백 확인 가능
- `apiFetch`가 비-JSON 응답(HTML 에러 페이지 등)에도 안전하게 동작

### 개인정보 / 보안
- 시청자 식별자(`sender_channel_id`)를 SHA-256 해시(16자)로 익명화
- CSV 수식 인젝션 방지 (`=` `+` `-` `@` 시작 셀에 `'` 접두)

### 사용성
- 수집 중 웹 화면에 "마지막 채팅 수신 시각" 표시 (새로고침으로 정상 수집 확인)
- 새 수집 시작 시 이전 방송의 수신 시각/최근 채팅 초기화

### 구조
- CLI 수집기(`collector.js`)가 `ChatCollector` 클래스를 재사용하도록 정리 (중복 로직 제거)
- CLI 토큰 갱신 시 `.tokens.latest.json` 저장 복원 (콘솔 출력만으로는 토큰 유실 위험)

## 2026-06-09

- 최초 버전: 치지직 공식 Open API Session 기반 라이브 채팅 수집기
- OAuth 연동 로컬 웹앱(`server.js`), CLI 수집기(`collector.js`)
- 자동 저장 예약(standby) 모드: 방송 시작 감지 후 자동 수집, 30초 간격 재시도
- CSV/JSONL 동시 저장, 첫 수집 테스트 완료
